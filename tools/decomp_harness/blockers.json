{
  "_comment": "Hand-curated blocker registry. 'files_blocked' lists asm files whose decomp failed on this blocker; gating estimates (which pending files this blocker likely also affects) are computed by coverage_ledger.py from the symbol graph at generation time. Keep fix_plan current — fixing the blocker that gates the most files is worth more than individual decomps.",
  "blockers": [
    {
      "id": "ipa-shared-headers",
      "description": "MWCC -ipa file: changing a signature in a shared header cascades codegen changes into every already-matched caller in other compilation units. Files sharing many declarations (sound.h family) must be decompiled together or after a coordinated header fix.",
      "files_blocked": ["asm/unk_02005D10.s", "asm/unk_0200FA24.s"],
      "fix_plan": "Identify the header-sharing cluster (sound.h / sound_chatot.h consumers), fix all signatures in one change, and re-match affected files in a single commit. Alternatively decompile the cluster as a group."
    },
    {
      "id": "param-copyprop-cmp",
      "description": "MWCC copy-propagates parameter copies: 'adds r4, r0, #0; cmp r4, #N' at function entry cannot be produced from pure C (MWCC substitutes back to r0). Affected functions need the NONMATCHING inline-asm fallback.",
      "files_blocked": ["asm/unk_0200B150.s"],
      "fix_plan": "Use NONMATCHING inline asm for the single affected function (OamManager_Create) and re-land the file with the other 10/11 matched functions in C."
    },
    {
      "id": "objdiff-false-positives",
      "description": "RESOLVED. objdiff.py had a critical bug: the byte extraction regex did not match MWCC's ARM Thumb objdump format (packed hex like 'b418' vs expected space-separated 'b4 18'). It extracted 0 bytes for every function, so 0==0 always reported MATCH. 11 decomps accepted via objdiff were not actually byte-matching. Fixed in this session; all 11 non-matching decomps reverted to asm. 2 decomps that truly match (unk_0202DB34, battle_arcade_game_board_data) kept.",
      "files_blocked": [],
      "fix_plan": "No further action. The objdiff tool is now fixed. Future decomps will be properly byte-verified."
    }
  ]
}
